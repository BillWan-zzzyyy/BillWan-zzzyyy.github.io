---
layout: default
---
<div class="post about-home">
  <div class="about-hero">
    <header class="post-header about-header about-hero__header">
      <h1 class="post-title about-title">
        {% if site.title == 'blank' %}
          <span class="font-weight-bold">{{ site.first_name }}</span> {{ site.middle_name }}
          {{ site.last_name }}
        {% else %}
          {{ site.title }}
        {% endif %}
      </h1>

      <div class="desc about-subtitle">{{ page.subtitle }}</div>
      <p class="about-positioning">{{ page.positioning }}</p>
      <nav class="about-actions" aria-label="Explore my work">
        <a class="about-action" href="{{ '/publications/' | relative_url }}">Publications</a>
        <a class="about-action" href="{{ '/projects/' | relative_url }}">Projects</a>
      </nav>

      {% if page.social %}
        <div class="social about-social">
          <div class="contact-icons">{% include social.liquid %}</div>
          <div class="contact-note">{{ site.contact_note }}</div>
        </div>
      {% endif %}

      {% if site.newsletter.enabled and site.footer_fixed %}
        {% include scripts/newsletter.liquid center=true %}
      {% endif %}
    </header>

    {% if page.profile %}
      <div class="about-hero__media">
        <div class="profile">
          <div class="profile__card">
            {% if page.profile.image %}
              {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
              {% if page.profile.image_circular %}
                {% assign profile_image_class = 'img-fluid rounded-circle' %}
              {% else %}
                {% assign profile_image_class = 'img-fluid rounded' %}
              {% endif %}
              {% capture sizes %}(min-width: 768px) 280px, 180px{% endcapture %}
              {%
                include figure.liquid
                loading="eager"
                fetchpriority="high"
                width="1000"
                height="1000"
                path=profile_image_path
                class=profile_image_class
                sizes=sizes
                alt=page.profile.alt
                cache_bust=true
              %}
            {% endif %}
            {% if page.profile.more_info %}
              <div class="more-info">{{ page.profile.more_info }}</div>
            {% endif %}
          </div>
        </div>
      </div>
    {% endif %}

    <div class="bio about-hero__bio">{{ content }}</div>
  </div>

  <article class="about-home__content">
    {% if page.research %}
      <section class="home-section research-block" data-reveal>
        <h2 class="home-section__title">Research</h2>
        {% assign rpath = page.research.image | prepend: 'assets/img/' %}
        {% include figure.liquid path=rpath class="img-fluid rounded research-block__figure" width="1600" height="1034" sizes="(min-width: 1120px) 984px, calc(100vw - 80px)" alt=page.research.alt zoomable=true %}
        <div class="row">
          {% for g in page.research.groups %}
            <div class="col-md-6">
              <p class="research-block__group">{{ g.title }}</p>
              <ul>
                {% for item in g.items %}
                  <li>{{ item }}</li>
                {% endfor %}
              </ul>
            </div>
          {% endfor %}
        </div>
      </section>
    {% endif %}

    {% if page.selected_papers %}
      <section class="home-section selected-publications" data-reveal>
        <h2 class="home-section__title">
          <a href="{{ '/publications/' | relative_url }}">Selected Publications</a>
        </h2>
        {% include selected_papers.liquid %}
        <p class="home-more">
          <a href="{{ '/publications/' | relative_url }}">All publications →</a>
        </p>
      </section>
    {% endif %}

    {% if page.news and site.announcements.enabled %}
      <section class="home-section recent-news" data-reveal>
        <h2 class="home-section__title">
          <a href="{{ '/news/' | relative_url }}">Recent News</a>
        </h2>
        {% include news.liquid limit=true %}
      </section>
    {% endif %}
  </article>

  <div class="footer__ornament">
    <div class="footer__ornament-map">
      <script
        async
        type="text/javascript"
        id="clustrmaps"
        src="//clustrmaps.com/map_v2.js?d=iWTm2HbayeimtnXDADURqs5_Im3_wek8M_WS3lq5qLI&cl=ffffff&w=a"
      ></script>
    </div>
  </div>
</div>
